import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const rabSlice = createSlice({
  name: "rab",
  initialState,
  reducers: {},
});

export const {} = rabSlice.actions;

export default rabSlice.reducer;
